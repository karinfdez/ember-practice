import Ember from 'ember';

export default Ember.Route.extend({
	// Shows different template when typing /schools(loads application.hbs)
	// renderTemplate(){
	// 	this.render('application');
	// }
});
